"""
Wrap Module

Wrapper functions.
"""

from typing import Callable, Any


def wrap(func: Callable) -> Callable:
    """Wrap a function."""
    return func


def unwrap(func: Callable) -> Callable:
    """Unwrap a function."""
    return func
